function [X1, X2, X3] = generate_data()
%Generating data
% 3 groups of sample, each size is 20
N_in_group = 40;
N_group = 3;
X1=zeros(0, N_in_group ,2);
X2=zeros(0, N_in_group ,2);
X3=zeros(0, N_in_group ,2);

theta = rand(1,N_in_group)*2*pi
x[,1] = (r  ) * cos(theta) + rnorm(N_total, sd = .2)
x[,2] = (r  ) * sin(theta) + rnorm(N_total, sd = .2)


end

